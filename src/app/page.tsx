import {prisma} from '@/lib/db';export const dynamic='force-dynamic';
async function stats(){try{const[jobs,qualified,applications,followups,recent]=await Promise.all([prisma.job.count(),prisma.job.count({where:{status:'QUALIFIED'}}),prisma.application.count({where:{status:'SUBMITTED'}}),prisma.followup.count({where:{status:'PENDING',dueAt:{lte:new Date()}}}),prisma.job.findMany({orderBy:[{score:'desc'},{createdAt:'desc'}],take:8})]);return{jobs,qualified,applications,followups,recent,ok:true}}catch{return{jobs:0,qualified:0,applications:0,followups:0,recent:[],ok:false}}}
export default async function Home(){const s=await stats();return <><div className="eyebrow">Evidence-first automation</div><h1 className="title">Job Search Command Center</h1><p className="sub">Discover public roles, verify availability, score against a private candidate profile, create claim-safe resume drafts, and track every application and follow-up.</p>{!s.ok&&<p className="notice">Database is not connected. Start Docker services and run the Prisma migration.</p>}<div className="grid">{[['Jobs found',s.jobs],['Qualified',s.qualified],['Submitted',s.applications],['Follow-ups due',s.followups]].map(([l,v])=><div className="card" key={String(l)}><div className="metric">{v}</div><div className="label">{l}</div></div>)}</div><section className="section"><div className="section-head"><h2>Top opportunities</h2><a className="btn secondary" href="/jobs">View all</a></div><div className="table-wrap"><table className="table"><thead><tr><th>Role</th><th>Company</th><th>Location</th><th>Score</th><th>Status</th></tr></thead><tbody>{s.recent.map(j=><tr key={j.id}><td><a href={j.url} target="_blank" rel="noreferrer">{j.title}</a></td><td>{j.company}</td><td>{j.location??'—'}</td><td><span className={`badge ${j.matchClass??'D'}`}>{j.score??'—'}</span></td><td>{j.status}</td></tr>)}</tbody></table>{!s.recent.length&&<div className="empty">No jobs yet. Add an ATS source on the Jobs page.</div>}</div></section></>}
