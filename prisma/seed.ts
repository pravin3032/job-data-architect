import {PrismaClient} from '@prisma/client';import {loadCandidateProfile} from '../src/lib/candidate';import {scoreJob} from '../src/lib/scoring';
const prisma=new PrismaClient();const profile=await loadCandidateProfile('config/candidate.example.json');
const samples=[{externalId:'sample-lead-architect',title:'Lead Data Architect',company:'Example Financial',location:'Remote, US',url:'https://example.com/jobs/lead-data-architect',description:'Lead Snowflake data platform modernization using dbt, Airflow, AWS, Python, SQL, governance, CI/CD and data modeling.',salaryMin:180000,salaryMax:205000},{externalId:'sample-engineer',title:'Senior Data Engineer',company:'Example Manufacturing',location:'Hybrid',url:'https://example.com/jobs/senior-data-engineer',description:'Build Databricks, Spark and AWS pipelines for supply chain analytics.',salaryMin:145000,salaryMax:170000}];
for(const sample of samples){const result=scoreJob(sample,profile);await prisma.job.upsert({where:{source_externalId:{source:'MANUAL',externalId:sample.externalId}},update:{...sample,score:result.score,matchClass:result.matchClass,scoreProof:result},create:{...sample,source:'MANUAL',score:result.score,matchClass:result.matchClass,scoreProof:result,status:result.score>=65?'QUALIFIED':'FOUND'}})}
for(const [name,focus] of [['Base - Snowflake & Modern ELT','Snowflake & Modern ELT'],['Base - Enterprise Data Modeling','Enterprise Data Modeling'],['Base - Data & AI Architecture','Data & AI Architecture'],['Base - Enterprise Data Architecture','Enterprise Data Architecture']])await prisma.resumeVersion.upsert({where:{name},update:{focus},create:{name,focus}});
console.log('Seed complete');await prisma.$disconnect();
